#include <stdio.h>
#include "header.h"

int main(void){
	ParityArray* adat = NULL;
	initParityArray(adat);

	printParityArray(adat);

	// insertToParityArray(adat, 2);
	// printParityArray(adat);
	// insertToParityArray(adat, 4);
	// printParityArray(adat);
	// insertToParityArray(adat, 9);
	// printParityArray(adat);
	// insertToParityArray(adat, 2);
	// printParityArray(adat);
	// insertToParityArray(adat, 11);
	// printParityArray(adat);
	return 0;
}
